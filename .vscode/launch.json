{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug clipboard-manager",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/clipboard-manager",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: Build"
        }
    ]
}
